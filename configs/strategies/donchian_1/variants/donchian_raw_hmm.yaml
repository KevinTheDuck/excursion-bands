active: true

metadata:
  label: donchian_raw_hmm
  description: Raw Donchian breakout filtered by HMM regime state.

configuration:
  side_mode: long
  use_atr_buffer: false
  use_vwap_filter: false
  use_band_filter: false
  use_hmm_filter: true
