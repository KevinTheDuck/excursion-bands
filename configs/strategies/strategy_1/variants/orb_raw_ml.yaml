active: true

metadata:
  label: orb_band_plus_ml
  description: Excursion band ORB augmented with high-confidence raw ORB ML candidates.

configuration:
  side_mode: long
  use_atr_buffer: false
  use_vwap_filter: false
  use_band_filter: true
  use_ml_filter: true
  ml_execution_mode: additive
  ml_candidate_scope: raw
